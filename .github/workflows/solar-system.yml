name: solar-system workflow
on: 
    workflow_dispatch:
    push:
        branches:
          - main
          - 'feature/*'
env:
    MONGO_URI: 'mongodb+srv://supercluster.d83jj.mongodb.net/superData'
    MONGO_USERNAME: ${{ secrets.MONGO_USERNAME }}
    MONGO_PASSWORD: ${{ secrets.MONGO_PASSWORD }}
jobs: 
    unit-testing:
        name: unit-testing
        strategy:
            matrix:
                node-js-versions: [18,19,20]
                os: [ubuntu-latest]
                # exclude:
                #     - node-js-versions: 18
                #       os: macos-latest
        runs-on: ${{ matrix.os }}
        steps:
          - name: checkout-repo
            uses: actions/checkout@v4
          - name: setup node
            uses: actions/setup-node@v6
            with: 
                node-version: ${{ matrix.node-js-versions }}
          - name: cache the dependencies
            uses: actions/cache@v4
            with:
                key: ${{ matrix.os }}-node_modules-${{ hashFiles('package-lock.json')}}
                path: node_modules
          - name: installing dependencies
            run: npm install
          - name: run unit tests
            run: npm test
          - name: save test results artifacts
            uses: actions/upload-artifact@v4
            with:
                name: results-${{ matrix.os }}-${{ matrix.node-js-versions }}
                path: test-results.xml
    coverage-testing:
        name: coverage-testing
        strategy:
            matrix:
                node-js-versions: [18,19,20]
                os: [ubuntu-latest]
                # exclude:
                #     - node-js-versions: 18
                #       os: macos-latest
        runs-on: ${{ matrix.os }}
        steps:
          - name: checkout-repo
            uses: actions/checkout@v4
          - name: setup node
            uses: actions/setup-node@v6
            with: 
                node-version: ${{ matrix.node-js-versions }}
          - name: cache the dependencies
            uses: actions/cache@v4
            with:
                key: ${{ matrix.os }}-node_modules-${{ hashFiles('package-lock.json')}}
                path: node_modules
          - name: installing dependencies
            run: npm install
          - name: run coverage tests
            continue-on-error: true
            run: npm run coverage
          - name: save coverage results artifacts
            uses: actions/upload-artifact@v4
            with:
                name: coverage_results-${{ matrix.os }}-${{ matrix.node-js-versions }}
                path: coverage
    docker:
        name: containerization
        needs: [unit-testing, coverage-testing]
        runs-on: ubuntu-latest
        permissions: 
          packages: write
        steps:
          - name: checkout-repo
            uses: actions/checkout@v4
          - name: Docker login
            uses: docker/login-action@v4
            with:
                username: ${{ vars.DOCKERHUB_USERNAME }}
                password: ${{ secrets.DOCKERHUB_TOKEN }}
          - name: GHCR login
            uses: docker/login-action@v4
            with:
                registry: ghcr.io
                username: ${{ github.repository_owner }}
                password: ${{ secrets.GITHUB_TOKEN }}
          - name: build the image
            uses: docker/build-push-action@v7
            with:
              context: .
              push: false
              tags: ${{ vars.DOCKERHUB_USERNAME }}/solar-system:${{github.sha}}
          - name: test the image
            run: |
              docker images
              docker run --name solar-system-app -d \
                -p 3000:3000 \
                -e MONGO_URI=$MONGO_URI \
                -e MONGO_USERNAME=$MONGO_USERNAME \
                -e MONGO_PASSWORD=$MONGO_PASSWORD \
                ${{ vars.DOCKERHUB_USERNAME }}/solar-system:${{github.sha}}
              export IP=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' solar-system-app)
              echo $IP

              echo testing image url using wget
              wget -q -O - 127.0.0.1:3000/live | grep live
          - name: push the image
            uses: docker/build-push-action@v7
            with:
              context: .
              push: true
              tags: |
                ${{ vars.DOCKERHUB_USERNAME }}/solar-system:${{github.sha}}
                ghcr.io/${{ github.repository_owner }}/solar-system:${{github.sha}}
    connect_to_GKE_and_Deploy:
        runs-on: ubuntu-latest
        needs: docker
        permissions:
          contents: read
          id-token: write 
        outputs: 
          App_Ingress_IP: ${{ steps.ingress.outputs.APP_INGRESS_HOST }}
        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Authenticate with Google Cloud
            uses: google-github-actions/auth@v2
            with:
              workload_identity_provider: projects/1045795533938/locations/global/workloadIdentityPools/github-pool/providers/github-provider
              service_account: github-actions@project-fb312730-ed01-42b7-924.iam.gserviceaccount.com

          - name: Get GKE credentials
            uses: google-github-actions/get-gke-credentials@v2
            with:
              cluster_name: nodejs-cluster
              location: asia-south1-a
              project_id: project-fb312730-ed01-42b7-924

          - name: Verify cluster access
            run: |
              kubectl get nodes
          - name: Create namespace
            run: |
              kubectl create namespace solar-system \
              --dry-run=client -o yaml | kubectl apply -f -        
          
          - name: Create MongoDB secret
            run: |
              kubectl -n solar-system create secret generic mongo-db-creds \
                --from-literal=MONGO_URI="${{ env.MONGO_URI }}" \
                --from-literal=MONGO_USERNAME="${{ vars.MONGO_USERNAME }}" \
                --from-literal=MONGO_PASSWORD="${{ secrets.MONGO_PASSWORD }}" \
                --save-config \
                --dry-run=client \
                -o yaml | kubectl apply -f -

          - name: Deploy to GKE
            env:
              NAMESPACE: solar-system
              REPLICAS: "2"
              K8S_IMAGE: docker.io/${{ vars.DOCKERHUB_USERNAME }}/solar-system:${{github.sha}}
              INGRESS_IP: "34.123.45.78"
            run: |
              cat kubernetes/development/*.yaml | envsubst
              cat kubernetes/development/*.yaml | envsubst | kubectl apply -f -
          - name: Wait for rollout
            run: |
              kubectl rollout status deployment/solar-system \
              -n solar-system \
              --timeout=120s
          - name: save ingress IP 
            id: ingress 
            run: |
              echo "APP_INGRESS_HOST=$(kubectl -n solar-system get ingress -o jsonpath="{.items[0].spec.tls[0].hosts[0]}")" >> $GITHUB_OUTPUT
    Integration_test:
        runs-on: ubuntu-latest
        needs: connect_to_GKE_and_Deploy
        env:
          Ingress_IP: ${{ needs.connect_to_GKE_and_Deploy.outputs.App_Ingress_IP }}

        steps:
          - name: Checkout
            uses: actions/checkout@v4
          - name: Install jq
            run: sudo apt-get update && sudo apt-get install -y jq
          - name: Test the ingress 
            run: |
              echo $Ingress_IP
              echo ----------------
              curl http://$Ingress_IP/live -s -k | grep -i live






