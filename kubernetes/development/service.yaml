---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: solar-system
  name: solar-system
  namespace: "${NAMESPACE}"
spec:
  type: NodePort
  selector:
    app: solar-system
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    
   